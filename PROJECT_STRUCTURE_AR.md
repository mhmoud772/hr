# هيكلة مشروع HR Companion

هذا المستند يوضح الهيكلية الكاملة لمشروع **HR Companion**، وهو نظام لإدارة الموارد البشرية يعتمد على معمارية المونوليث النمطي (Modular Monolith).

## 📂 الهيكل العام للمجلدات

```text
hr-companion-main/
├── 📂 backend/              # الكود المصدري للخلفية (Django/Python)
├── 📂 frontend/             # الكود المصدري للواجهة الأمامية (React/Vite)
├── 📂 nginx/                # إعدادات خادم Nginx للإنتاج
├── 📂 ops/                  # ملفات العمليات والـ DevOps
├── 📂 tools/                # أدوات مساعدة ونصوص برمجية (Scripts)
├── 📄 docker-compose.yml    # ملف تشغيل الحاويات (Docker)
├── 📄 ARCHITECTURE_AR.md    # توثيق المعمارية باللغة العربية
└── 📄 README.md             # دليل البدء السريع
```

---

## 🐍 الجزء الخلفي (Backend)

يعتمد الجزء الخلفي على **Django Custom Architecture** لضمان الفصل بين النطاقات (Domains).

```text
backend/
├── 📂 apps/                 # التطبيقات الوظيفية للنظام
│   ├── 📂 ai/               # محرك تحليل البيانات والذكاء الاصطناعي
│   ├── 📂 attendance/       # إدارة الحضور والانصراف والورديات
│   ├── 📂 authentication/   # إدارة المستخدمين والصلاحيات (JWT/RBAC)
│   ├── 📂 devices/          # التكامل مع أجهزة البصمة (ZKTeco)
│   ├── 📂 employees/        # إدارة بيانات الموظفين والهيكل التنظيمي
│   ├── 📂 leaves/           # إدارة الإجازات والطلبات
│   ├── 📂 reports/          # محرك التقارير والبيانات التحليلية
│   └── 📂 system/           # إعدادات النظام والمراقبة
├── 📂 config/               # إعدادات مشروع Django الأساسية
├── 📂 shared/               # الأكواد المشتركة (Base Models, Utilities)
├── 📂 scripts/              # نصوص برمجية لأتمتة المهام
├── 📄 manage.py            # أداة إدارة Django
└── 📄 requirements.txt      # المكتبات المطلوبة للتشغيل
```

---

## ⚛️ الواجهة الأمامية (Frontend)

تستخدم الواجهة الأمامية **React** مع **TypeScript** و **Vite**، وتتبع نظام الـ Features.

```text
frontend/
├── 📂 src/
│   ├── 📂 features/         # الوحدات الوظيفية (كل وحدة تحتوي على مكوناتها ومنطقها)
│   │   ├── 📂 auth/         # شاشات تسجيل الدخول والتحقق
│   │   ├── 📂 dashboard/    # لوحة التحكم الرئيسية
│   │   ├── 📂 employees/    # إدارة الموظفين
│   │   ├── 📂 attendance/   # عرض سجلات الحضور
│   │   └── ...              # (بقية الميزات: التقارير، الإجازات، إلخ)
│   ├── 📂 shared/           # المكونات المشتركة (UI Elements, Layouts)
│   ├── 📂 hooks/            # الخطافات المخصصة (Custom Hooks)
│   ├── 📂 types/            # تعريفات الأنواع (TypeScript Interfaces)
│   ├── 📂 i18n/             # ملفات الترجمة (دعم العربية والإنجليزية)
│   ├── 📄 App.tsx           # المكون الرئيسي والمسارات (Routing)
│   └── 📄 main.tsx          # نقطة الدخول للتطبيق
├── 📂 public/               # الملفات الثابتة (أيقونات، صور)
├── 📄 package.json          # المكتبات والتبعيات
└── 📄 vite.config.ts        # إعدادات Vite
```

---

## 🛠️ أدوات إضافية وملفات هامة

- **`.env`**: ملف يحتوي على المتغيرات البيئية السرية.
- **`ARCHITECTURE.md`**: تفاصيل تقنية حول كيفية عمل النظام داخلياً.
- **`docker-compose.prod.yml`**: إعدادات التشغيل لبيئة الإنتاج.
- **`schema.yml`**: توثيق الـ API (Swagger/OpenAPI).
