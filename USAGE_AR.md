# دليل استخدام نظام HR Companion

هذا الدليل يشرح طريقة استخدام النظام بشكل عملي من البداية حتى التشغيل اليومي.

## 1. قبل البدء

- تأكد من عمل الخادم الخلفي والواجهة الأمامية وقاعدة البيانات.
- افتح النظام باستخدام متصفح مدعوم (يُفضل إصدارات حديثة من Google Chrome أو Microsoft Edge لضمان أفضل توافقية وتجربة مستخدم).
- يدعم النظام التصفح عبر الأجهزة المحمولة بفضل تصميمه المتجاوب.
- إذا كانت هذه أول مرة تشغيل، سيحولك النظام تلقائيًا إلى صفحة الإعداد الأولي.

## 2. الإعداد الأولي (First Run)

عند التشغيل لأول مرة فقط:

1. ادخل بيانات حساب المدير الرئيسي:
   - اسم المستخدم
   - البريد الإلكتروني
   - كلمة المرور
2. أدخل بيانات الشركة الأساسية (اختياري حسب الحقول الظاهرة).
3. احفظ الإعداد.

بعد الإكمال:

- ينشأ أول مستخدم إداري.
- يتم تفعيل `setupCompleted`.
- لا يمكن فتح صفحة الإعداد الأولي مرة أخرى.
- يعمل النظام بعد ذلك بآلية تسجيل الدخول والصلاحيات العادية.

## 3. تسجيل الدخول وإدارة الحساب

- تسجيل الدخول من صفحة `/login`.
- استعادة كلمة المرور من:
  - `/reset-password`
  - `/reset-password/confirm`
- تغيير كلمة المرور من داخل النظام عبر `/change-password`.
- عند انتهاء الجلسة سيطلب النظام تسجيل الدخول مرة أخرى تلقائيًا.

## 4. هيكل التنقل داخل النظام

القائمة الجانبية الرئيسية تتضمن:

- لوحة التحكم: `/`
- التقارير: `/reports`
- الموظفون: `/employees`
- الهيكل الإداري: `/structure`
- المسميات الوظيفية: `/job-titles`
- الرواتب: `/payroll`
- الإجازات: `/leaves`
- الحضور: `/attendance`
- التوظيف: `/recruitment`
- الأداء: `/performance`
- التدريب: `/training`
- الأصول: `/assets`
- الأجهزة: `/devices`
- الخدمة الذاتية: `/self-service`
- المستخدمون: `/users`
- سجل التدقيق: `/audit-logs`
- الإعدادات: `/settings`

ملاحظات:

- إدارة الصلاحيات تتم من شاشة المستخدمين والأدوار داخل النظام.

## 5. طريقة الاستخدام حسب كل شاشة

### 5.1 لوحة التحكم `/`

- متابعة المؤشرات العامة السريعة.
- الوصول للتنبيهات والإجراءات المتكررة.

### 5.2 التقارير `/reports`

- تصفية البيانات حسب الفترة أو القسم.
- تصدير النتائج (PDF / CSV / Excel حسب المتاح في الشاشة).

### 5.3 الموظفون `/employees`

- إضافة موظف جديد.
- تعديل بيانات الموظف.
- إرفاق مستندات الموظف.
- أرشفة/حذف الموظف حسب الصلاحيات.
- استيراد الموظفين دفعة واحدة عند الحاجة.

### 5.4 الهيكل الإداري `/structure`

- إنشاء الأقسام.
- ربط الأقسام بعلاقة أب/ابن.
- إعادة ترتيب الأقسام بالسحب والإفلات.

### 5.5 المسميات الوظيفية `/job-titles`

- إنشاء مسمى وظيفي.
- تعديل المسمى.
- ربط المسمى بالموظفين أو الأقسام.

### 5.6 الرواتب `/payroll`

- متابعة سجلات الرواتب.
- مراجعة الحالات الشهرية.
- فلترة وتصدير البيانات.

### 5.7 الإجازات `/leaves`

- تقديم طلب إجازة.
- اعتماد/رفض الطلب (للمدير أو المسؤول).
- متابعة رصيد الإجازات وسجل الطلبات.

### 5.8 الحضور `/attendance`

- إعداد وإدارة جداول العمل والورديات (Shifts & Schedules) للموظفين.
- عرض سجلات الحضور والانصراف بناءً على الورديات المعينة.
- تصحيح السجلات يدويًا وتسجيل الأعذار إذا لزم الأمر.
- إغلاق اليوم وتحديث السجلات لاحتساب التأخير والغياب بدقة.
- استيراد/تصدير بيانات الحضور.

### 5.9 التوظيف `/recruitment`

- تسجيل المرشحين.
- تحديث مراحل التوظيف.
- متابعة حالة كل مرشح حتى الإغلاق.

### 5.10 الأداء `/performance`

- إنشاء تقييمات الأداء.
- تسجيل الدرجات والملاحظات.
- مراجعة نتائج الموظفين حسب الفترة.

### 5.11 التدريب `/training`

- إنشاء البرامج التدريبية.
- ترشيح الموظفين.
- متابعة حالة الإتمام والتقدم.

### 5.12 الأصول `/assets`

- تسجيل الأصول.
- ربط الأصل بموظف.
- متابعة الحالة (نشط/تالف/مفقود...).

### 5.13 الأجهزة `/devices`

- إضافة أجهزة البصمة المدعومة (مثل ZKTeco وغيرها) عبر (IP/Port/Comm Key).
- اختبار الاتصال مع الجهاز والتأكد من حالة الشبكة.
- سحب ومزامنة الحركات إما يدوياً (عبر زر المزامنة) أو الاستعلام عن الجدولة التلقائية في النظام.
- مزامنة الحالة وسجلات الحضور.
- تنفيذ أوامر الجهاز الفنية (إعادة تشغيل، مسح السجلات، مزامنة الوقت) حسب الصلاحيات.

### 5.14 الخدمة الذاتية `/self-service`

واجهة مخصصة للموظف:

- تحديث بياناته الشخصية.
- عرض حضوره وإجازاته.
- متابعة الطلبات والتنبيهات الخاصة به.

### 5.15 المستخدمون والصلاحيات `/users`

- إنشاء مستخدم نظام جديد.
- تحديد الدور وتوضيح مستوى الوصول كالتالي:
  - **`system_admin` / `admin`**: تحكم كامل بالنظام والإعدادات المتقدمة والمستخدمين.
  - **`hr_manager`**: إدارة شؤون الموظفين، الرواتب، الإجازات، وتكوين الهيكل الإداري بالكامل.
  - **`supervisor` (المدير المباشر)**: يرى بيانات وحضور موظفي قسمه فقط، ويعتمد أو يرفض إجازاتهم وطلباتهم.
  - **`employee` (الموظف العادي)**: وصول مقيد بشاشة الخدمة الذاتية (Self-Service) لمتابعة سجلاته وتقديم طلباته.
- تخصيص الصلاحيات المتقدمة بشكل فردي أو استخدام صلاحيات الدور الافتراضية.
- تعطيل المستخدم أو حذفه حسب الصلاحية.

### 5.16 سجل التدقيق `/audit-logs`

- تتبع كل العمليات المهمة في النظام.
- مراجعة من قام بالإجراء ومتى.
- دعم التحقيق في المشاكل والأخطاء التشغيلية.

### 5.17 الإعدادات `/settings`

- إعدادات الشركة العامة.
- إعدادات اللغة/المنطقة الزمنية.
- إعدادات الحضور والإجازات.
- إعدادات الأمان (كلمات المرور وMFA ومفاتيح الأمان حسب التفعيل).
- إعدادات الإشعارات والتكاملات.

## 6. دورات العمل الأساسية والتشغيل اليومي (Workflows & Daily Operations)

النظام مصمم ليخدم دورات العمل المتكاملة في الموارد البشرية. إليك بعض السيناريوهات الشائعة وسير العمل اليومي:

### 6.1 دورات العمل المتسلسلة (Business Workflows)

- **دورة تعيين موظف جديد (Onboarding):**
  1. إضافة بيانات الموظف الأساسية وإنشاء ملفه في `/employees`.
  2. ربط الموظف بالقسم وتحديد مسماه الوظيفي.
  3. إعداد الراتب وتفاصيل الدفع في `/payroll`.
  4. تسليم العهد والأصول وتقييدها في `/assets`.
  5. إنشاء حساب مستخدم للموظف للدخول إلى الخدمة الذاتية في `/users`.
  6. تسجيل بصمة الموظف في الجهاز وربطه بالنظام في `/devices`.

- **دورة إنهاء الخدمة أو الاستقالة (Offboarding):**
  1. أرشفة/إيقاف الموظف في النظام لتعليق عملياته المستقبلية.
  2. استلام الأصول والعهد وتحديث حالتها.
  3. إيقاف تنشيط حساب التوثيق الخاص للولوج للخدمة الذاتية.
  4. تصفية رواتب الشهر الأخير وإغلاق ملفه المالي.

- **نظام الإشعارات والتنبيهات:**
  - يتم إرسال تنبيهات داخلية لإبقاء الأطراف المعنية على اطلاع (مثل إشعار المدير بطلب إجازة جديد، وتنبيه الموظف بعد الاعتماد).
  - يمكن متابعتها باستمرار من خلال أيقونة الإشعارات في النظام.

### 6.2 التشغيل والمتابعة اليومية

1. **مدير النظام (System Admin):**
   - يراجع لوحة التحكم والتنبيهات العامة.
   - يراجع سجل التدقيق عند أي تغيير حساس أو مشكلة تقنية.
2. **شؤون الموظفين (HR Manager):**
   - تحديث بيانات وملفات الموظفين (والسياسات العامة).
   - مراجعة نواقص الحضور وإغلاق الحركات.
3. **المدير المباشر (Supervisor):**
   - اعتماد أو رفض الطلبات المعلقة (إجازات، أذونات) من خلال التنبيهات.
4. **الموظف (Employee):**
   - مراجعة إشعاراته، واستخدام الخدمة الذاتية لتسجيل الطلبات ومتابعة الأرصدة.

## 7. نسخ احتياطي واستعادة (تشغيلي)

من الخادم:

- نسخ احتياطي: `backend/ops/backup_db.sh`
- استعادة: `backend/ops/restore_db.sh`
- خدمة النظام (systemd): `backend/ops/hr-backend.service`

يفضل جدولة النسخ الاحتياطي يوميًا والتحقق من نجاحه عبر سجلات النظام.

## 8. معالجة المشاكل الشائعة

- تظهر صفحة غير مصرح بها:
  - تحقق من دور المستخدم وصلاحياته من `/users`.
- تظهر نصوص إنجليزية:
  - تأكد أن اللغة العربية مفعلة في الإعدادات.
  - أعد تحميل الصفحة تحميلًا قسريًا.
- الجهاز لا يتصل:
  - تحقق من `IP` و`Port` و`Comm Key`.
  - تحقق من الوصول الشبكي والمنفذ.
- لا يمكن فتح النظام بعد تشغيل جديد:
  - تأكد من اكتمال الإعداد الأولي وإنشاء أول مدير.

## 9. مرجع سريع للمسارات

- إعداد أولي: `/initial-setup`
- دخول: `/login`
- استعادة كلمة المرور: `/reset-password`
- تأكيد الاستعادة: `/reset-password/confirm`
- تغيير كلمة المرور: `/change-password`
- لوحة التحكم: `/`
- الإعدادات: `/settings`

---
